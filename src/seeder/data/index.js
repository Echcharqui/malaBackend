const { fakeUsers } = require('./fakeUsers')
const { fakeProducts } = require('./fakeProducts')
const { brands, categorys, genres, subCategorys } = require('./basics')
module.exports = { fakeUsers, fakeProducts, genres, categorys, brands, subCategorys }
